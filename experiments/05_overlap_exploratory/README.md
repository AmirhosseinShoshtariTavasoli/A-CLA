# Exploratory overlap experiments

Overlap work is an exploratory extension. Use `python -m acla --mode overlap ...` for the cleaned wrapper. Historical scripts and output workbooks are under `legacy/` and `results/reference_archive/overlap_refinement/`.
